spring:
  application:
    name: custom
  cloud:
    nacos:
      discovery:
        namespace:
        server-addr: 127.0.0.1:8848,192.168.0.121:8848
        username: nacos
        password: nacos
#      config:
#        namespace:
#        server-addr: ${spring.cloud.nacos.discovery.server-addr}
#        group: DEFAULT_GROUP
#        file-extension: properties
#        name: ${spring.application.name}
#        username: ${spring.cloud.nacos.discovery.username}
#        password: ${spring.cloud.nacos.discovery.password}
#        timeout: 3000
dubbo:
  application:
    name: custom
  cloud:
    subscribed-services: order
  registry:
    address: spring-cloud://127.0.0.1
  protocol:
    name: dubbo
    port: -1
    dispatcher: all
    threadpool: eager
    threadname: threadname
    threads: 100

  consumer:
    check: false
    version: 1.0.1


logging:
  level:
    root: debug